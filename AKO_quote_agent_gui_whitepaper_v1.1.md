# AKO_quote_agent GUI 白皮书

> **文档作者**: AKO_studio  
> **版本**: v1.1  
> **日期**: 2026-07-26  
> **框架**: PySide6 (Qt6)  
> **打包**: PyInstaller + auto-py-to-exe  
> **性质**: 顶层设计文档，指导 VS Code 实现  
> **状态**: 架构终审通过（Conditional Approval → v1.1 修订后通过）

---

## 一、为什么 AKO_quote_agent 必须用 PySide6

| 功能需求 | ttkbootstrap 能力 | PySide6 能力 |
|----------|-------------------|--------------|
| 多标签页 (TabWidget) | ❌ 无原生支持 | ✅ QTabWidget 开箱即用 |
| 复杂表格 (增删改查/排序/筛选) | ❌ 极弱 | ✅ QTableView + QStandardItemModel |
| PDF 预览内嵌 | ❌ 无 | ✅ QPdfView (Qt6.4+) |
| 工程量计算实时联动 | ❌ 事件弱 | ✅ Signal/Slot 机制成熟 |
| 打印/导出对话框 | ❌ 无 | ✅ QPrintDialog / QFileDialog |
| 图表展示 (报价趋势) | ❌ 无 | ✅ QtCharts 原生支持 |
| 停靠面板 (Dock) | ❌ 无 | ✅ QDockWidget 专业级 |
| 后台线程计算 | ❌ 无 | ✅ QThread + Worker 模式 |
| 文件系统监控 | ❌ 无 | ✅ QFileSystemWatcher |

**结论**: AKO_quote_agent 是业务级工具，非 PySide6 不可。

---

## 二、窗口架构设计

### 2.1 整体布局：主窗口 + 停靠面板

采用 **QMainWindow** 作为主容器，结构如下：

```
┌─────────────────────────────────────────────────────────────┐
│  [AKO Logo]  AKO_quote_agent v1.0    [新建] [打开] [保存] [导出PDF] [设置] │  ← MenuBar + ToolBar
├──────────┬──────────────────────────────────────────────────┤
│          │  ┌──────────┬──────────┬──────────┬──────────┐  │
│  项目    │  │ 项目信息 │ 工程量   │ 报价明细 │ 预览输出 │  │  ← QTabWidget (中央部件)
│  导航    │  │          │ 计算     │          │          │  │
│  面板    │  ├──────────┴──────────┴──────────┴──────────┤  │
│ (QTree)  │  │                                         │  │
│          │  │         当前标签页内容区                 │  │
│          │  │                                         │  │
│          │  └─────────────────────────────────────────┘  │
├──────────┴──────────────────────────────────────────────────┤
│  状态栏: [当前项目: 万峰林民宿] [计算状态: ✓] [总计: ¥128,450]   │  ← StatusBar
└─────────────────────────────────────────────────────────────┘
```

### 2.2 尺寸与行为

| 属性 | 设定值 |
|------|--------|
| 默认尺寸 | 1400 × 900 像素 |
| 最小尺寸 | 1200 × 700 像素 |
| 启动位置 | 屏幕居中 |
| 最大化 | 允许，组件自适应 |
| 图标 | AKO Logo (ako_logo.ico) |
| 标题栏 | `AKO_quote_agent v1.0 — [当前项目名称]` |
| **高 DPI 适配** | **必须启用** `Qt.AA_EnableHighDpiScaling` 和 `Qt.AA_UseHighDpiPixmaps`，否则 4K 屏下 1400×900 会极小，阿里字体模糊 |

---

## 三、AKO 品牌视觉规范（GUI 级）

### 3.1 色彩系统（直接调用 AKO 品牌色）

| 用途 | 色值 | 应用场景 |
|------|------|----------|
| 主背景 | #EBDAB9 (奶油金) | 窗口底色、侧边栏底色 |
| 面板背景 | #C3BE94 (冷暖灰) | 内容区卡片底色、表格交替行 |
| 强调色 | #A08C64 (琥珀金) | 选中项、高亮按钮、当前标签页下划线 |
| 标题/文字 | #231E1C (深棕黑) | 所有文字、图标、边框 |
| 高亮文字 | #B99F5F (熔金) | 总价数字、关键指标 |
| 危险/删除 | #8B4513 (深棕红) | 删除按钮、错误提示 |
| 成功 | #6B8E23 (橄榄绿) | 计算完成、保存成功 |

### 3.2 字体规范

| 层级 | 字体 | 字号 | 字重 |
|------|------|------|------|
| 窗口标题 | 阿里巴巴普惠体 | 14px | Bold |
| 标签页标题 | 阿里巴巴普惠体 | 12px | Medium |
| 表格内容 | 阿里巴巴普惠体 | 11px | Regular |
| 总价金额 | Monospace (等宽) | 18px | Bold |
| 状态栏 | 阿里巴巴普惠体 | 10px | Regular |

### 3.3 圆角与阴影

- 按钮圆角: 4px
- 卡片/面板圆角: 6px
- 输入框圆角: 3px
- 选中项: 1px 琥珀金边框 + 2px 内发光

---

## 四、四大标签页详细设计

### 4.1 标签页一：项目信息 (Project Info)

**功能**: 录入项目基础信息，作为后续计算的数据源。

**布局**: 两栏表单（左宽右窄）

```
┌─────────────────────────────┬─────────────────┐
│  项目基本信息                │  客户信息卡片    │
│  ┌───────────────────────┐  │  ┌───────────┐  │
│  │ 项目名称: [________]  │  │  │ 客户名称   │  │
│  │ 项目地址: [________]  │  │  │ [________] │  │
│  │ 项目类型: [下拉 ▼]    │  │  │ 联系电话   │  │
│  │   (住宅/商业/民宿/工业) │  │  │ [________] │  │
│  │ 建筑面积: [____] ㎡   │  │  │ 备注       │  │
│  │ 层数:     [____] 层   │  │  │ [多行文本] │  │
│  └───────────────────────┘  │  └───────────┘  │
│                             │                 │
│  墙板技术参数 (自动带入)     │  历史报价关联   │
│  ┌───────────────────────┐  │  ┌───────────┐  │
│  │ 产品标准: T/CECS 10154│  │  │ [列表]     │  │
│  │ 面密度: 138 kg/m²     │  │  │ 2026-06   │  │
│  │ 抗压强度: 5-10 MPa    │  │  │ 2026-04   │  │
│  │ 隔声: 46 dB           │  │  └───────────┘  │
│  │ 耐火: ≥4h             │  │                 │
│  └───────────────────────┘  │                 │
└─────────────────────────────┴─────────────────┘
```

**组件清单**:
- QLineEdit × 4: 项目名称、地址、面积、层数
- QComboBox × 1: 项目类型（预设枚举）
- QTextEdit × 1: 备注
- QLabel × N: 技术参数只读展示
- QPushButton: 「从模板新建」「保存项目信息」

---

### 4.2 标签页二：工程量计算 (Quantity Calculation)

**功能**: 按房间/楼层拆分，精确计算墙板用量。

**布局**: 上方工具栏 + 左侧房间树 + 右侧计算表格

```
┌─────────────────────────────────────────────────────────────┐
│ [添加楼层] [添加房间] [删除] [复制] [从CAD导入] [自动计算]    │  ← 工具栏
├──────────────┬──────────────────────────────────────────────┤
│  楼层/房间树  │  工程量明细表                                 │
│  ┌──────────┐  │  ┌────┬────────┬──────┬──────┬──────┬──────┐  │
│  │ 1F       │  │  │ 序号│ 房间   │ 墙类型│ 长度 │ 高度 │ 面积 │  │
│  │  ├ 客厅  │  │  │ 1   │ 客厅   │ 内墙  │ 12.5 │ 3.0  │ 37.5 │  │
│  │  ├ 卧室  │  │  │ 2   │ 主卧   │ 外墙  │ 8.0  │ 3.0  │ 24.0 │  │
│  │ 2F       │  │  │ 3   │ 次卧   │ 内墙  │ 6.5  │ 3.0  │ 19.5 │  │
│  └──────────┘  │  └────┴────────┴──────┴──────┴──────┴──────┘  │
│                │                                              │
│                │  快速计算区                                   │
│                │  ┌────────────────────────────────────────┐  │
│                │  │  内墙总面积: 125.5 ㎡                  │  │
│                │  │  外墙总面积:  86.0 ㎡                   │  │
│                │  │  门窗洞口扣减: -23.5 ㎡                 │  │
│                │  │  ─────────────────────────────────────  │  │
│                │  │  净用量:     188.0 ㎡                   │  │
│                │  │  损耗系数:   [1.05 ▼]                   │  │
│                │  │  实际用量:   197.4 ㎡                   │  │
│                │  └────────────────────────────────────────┘  │
└────────────────┴──────────────────────────────────────────────┘
```

**组件清单**:
- **QTreeView** (替代 QTreeWidget): 楼层/房间层级树，与表格共享数据模型
- **QTableView + QStandardItemModel**: 工程量明细（支持增删改、排序）
- **QSortFilterProxyModel**: 根据树节点选中的 room_id 过滤表格行
- QDoubleSpinBox: 长度/高度/面积输入（带单位后缀）
- QComboBox: 墙类型（内墙/外墙/分户墙/防火墙）
- QSpinBox: 损耗系数（默认 1.05，范围 1.0-1.15）
- QLabel: 实时汇总（字体 14px Bold，琥珀金色）

**数据模型关联（关键架构）**:
- 每个房间节点必须携带 `room_id`（UUID 或自增整数）
- 明细表每一行必须包含 `room_id` 外键
- 树节点选中 → `QSortFilterProxyModel.setFilterFixedString(room_id)` → 表格仅显示该房间数据
- **严禁**遍历全表手动过滤，必须使用 ProxyModel

**交互逻辑**:
1. 双击表格单元格 → 进入编辑模式
2. 修改长度/高度 → 面积自动计算（Signal/Slot 联动）
3. 点击「自动计算」→ **触发后台 QThread 计算引擎**，UI 显示进度条，完成后更新汇总区
4. 切换房间树节点 → ProxyModel 自动过滤，表格即时响应

**防抖机制（强制）**:
- 所有联动计算（长度/高度修改 → 面积重算）必须通过 `QTimer::singleShot(300ms, ...)` 防抖
- 用户在 SpinBox 按住加减号时，以毫秒级频率发射的信号会被合并，300ms 无新输入后才触发一次计算
- 否则上百个房间时 CPU 飙升，界面假死

---

### 4.3 标签页三：报价明细 (Quotation Detail)

**功能**: 基于工程量，自动套用单价，生成完整报价单。

**布局**: 上方汇总卡片 + 中间明细表格 + 下方费用构成

```
┌─────────────────────────────────────────────────────────────┐
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │
│  │ 材料费       │ │ 安装费       │ │ 其他费用     │          │
│  │ ¥24,150     │ │ ¥18,200     │ │ ¥3,500      │          │
│  │ (内墙122/㎡) │ │ (人工+辅材)  │ │ (运输+管理)  │          │
│  └─────────────┘ └─────────────┘ └─────────────┘          │
├─────────────────────────────────────────────────────────────┤
│  报价明细表                                                  │
│  ┌────┬────────────┬────────┬────────┬────────┬──────────┐ │
│  │ 序号│ 费用项目    │ 单位   │ 数量   │ 单价   │ 合价     │ │
│  │ 1   │ 内墙板     │ ㎡     │ 125.5  │ 122    │ ¥15,311  │ │
│  │ 2   │ 外墙板     │ ㎡     │ 86.0   │ 357    │ ¥30,702  │ │
│  │ 3   │ 安装人工   │ ㎡     │ 211.5  │ 60     │ ¥12,690  │ │
│  │ 4   │ 辅材       │ 项     │ 1      │ 5,510  │ ¥5,510   │ │
│  │ 5   │ 运输费     │ 项     │ 1      │ 2,500  │ ¥2,500   │ │
│  │ 6   │ 管理费     │ 项     │ 1      │ 1,000  │ ¥1,000   │ │
│  └────┴────────────┴────────┴────────┴────────┴──────────┘ │
├─────────────────────────────────────────────────────────────┤
│  费用构成饼图 (QtCharts)          │  总价汇总               │
│  ┌─────────────────────────────┐  │  ┌──────────────────┐  │
│  │      [饼图占位]             │  │  │  税前总价         │  │
│  │                             │  │  │  ¥67,713         │  │
│  │                             │  │  │  税率: [9% ▼]     │  │
│  │                             │  │  │  税后总价         │  │
│  │                             │  │  │  ¥73,807         │  │
│  │                             │  │  │  [熔金 24px Bold] │  │
│  └─────────────────────────────┘  │  └──────────────────┘  │
│                                   │                        │
│  [点击饼图扇区 → 高亮明细表对应行]  │                        │
└─────────────────────────────────────────────────────────────┘
```

**组件清单**:
- QGroupBox × 3: 材料费/安装费/其他费用汇总卡片
- QTableView: 报价明细（只读，颜色区分材料/人工/其他）
- **QPieSeries (QtCharts)**: 费用构成饼图
- **图例与钻取**: 饼图显示图例（Legend 位置：右侧），点击扇区 → `clicked` 信号 → `QTableView.selectRow(index)` 高亮对应费用行
- QComboBox: 税率选择（9% 建安 / 13% 材料 / 免税）
- QLabel: 税前/税后总价（最大字号，熔金色，加粗）
- QPushButton: 「重新计算」「应用折扣」「锁定报价」

**核心逻辑**:
- 内墙板单价: 122 元/㎡（硬编码，可从设置读取）
- 外墙板单价: 357 元/㎡（硬编码，可从设置读取）
- 数量自动从「工程量计算」页带入
- 修改税率 → 税后总价实时重算（同样带 300ms 防抖）
- 点击「锁定报价」→ 生成报价编号，禁止再编辑，状态栏显示 🔒

---

### 4.4 标签页四：预览输出 (Preview & Export)

**功能**: 预览最终报价单排版，导出 PDF/Excel/打印。

**布局**: 左侧输出控制 + 右侧预览区

```
┌────────────────────┬──────────────────────────────────────────┐
│  输出格式           │                                          │
│  ○ PDF 正式报价单   │                                          │
│  ○ Excel 明细表    │         QPdfView 预览区                   │
│  ○ 打印            │                                          │
│                    │         (QTextDocument → QPdfView)       │
│  模板选择           │                                          │
│  [标准模板 ▼]      │                                          │
│  [AKO 高端模板 ▼]  │                                          │
│                    │                                          │
│  公司信息           │                                          │
│  [自动带入 ▼]      │                                          │
│                    │                                          │
│  ┌──────────────┐  │                                          │
│  │ [生成预览]   │  │                                          │
│  │ [导出文件]   │  │                                          │
│  │ [直接打印]   │  │                                          │
│  └──────────────┘  │                                          │
└────────────────────┴──────────────────────────────────────────┘
```

**组件清单**:
- **QTextDocument**: 报价单内容渲染核心（HTML + CSS 模板）
- **QPdfView**: 预览区，直接显示 QTextDocument 渲染后的 PDF 内存流
- **QPrinter**: 导出 PDF 和打印的统一引擎
- QRadioButton × 3: 输出格式选择
- QComboBox × 2: 模板选择、公司信息选择
- QPushButton × 3: 生成预览 / 导出文件 / 直接打印
- QFileDialog: 导出时弹出保存路径选择
- QPrintDialog: 打印时弹出打印机选择

**WYSIWYG 保证（关键修正）**:
- **预览与生成必须使用同一渲染管线**: `QTextDocument` → `QPrinter` → PDF
- 预览时: `QTextDocument.print(QPrinter)` 输出到内存 `QByteArray`，再用 `QPdfView.loadFromData()` 显示
- 导出时: 同一 `QTextDocument` 实例，同一 `QPrinter` 配置，输出到文件
- **严禁**预览用 QPdfView 直接加载文件、生成用其他引擎，否则屏显与纸面不一致

**模板系统**:
- 标准模板: 简洁表格，适合内部审核
- AKO 高端模板: 带品牌色页眉页脚、水磨石纹理背景、熔金标题
- 模板文件存为 `.html` + CSS，由 QTextDocument 渲染后统一走 QPrinter 管线

---

## 五、左侧导航面板 (Project Navigator)

**功能**: 管理所有历史项目，快速切换。

**组件**: **QTreeView** + **QFileSystemWatcher**

```
┌──────────────┐
│ [搜索...]    │  ← QLineEdit 过滤
├──────────────┤
│ 进行中      │
│  ├ 万峰林民宿│
│  ├ 桐木岭商业│
│  └ 花溪别墅  │
│ 已归档      │
│  ├ 安顺工厂  │
│  └ 贵阳围墙  │
│ 模板库      │
│  ├ 标准住宅  │
│  ├ 商业综合体│
│  └ 民宿改造  │
└──────────────┘
```

**交互**:
- 单击项目 → 右侧加载该项目全部数据
- 右键项目 → 菜单：重命名 / 复制 / 删除 / 归档
- 双击模板 → 基于模板新建项目
- 搜索框输入 → 实时过滤项目名（QSortFilterProxyModel）

**文件系统监控（强制）**:
- 使用 `QFileSystemWatcher` 监控 `~/AKO_Projects/` 目录
- 当用户在外部（如 Obsidian、文件管理器）修改了项目 JSON，导航树自动刷新
- 外部修改的项目节点显示 **黄色感叹号图标**，提示「外部已变更，建议重新加载」
- 点击感叹号 → 弹出确认对话框 → 重新加载 JSON

---

## 六、顶部工具栏 (ToolBar)

**按钮组**（从左到右）：

| 图标 | 功能 | 快捷键 |
|------|------|--------|
| 📝 | 新建项目 | Ctrl+N |
| 📂 | 打开项目 | Ctrl+O |
| 💾 | 保存 | Ctrl+S |
| 📄 | 导出 PDF | Ctrl+E |
| 🖨️ | 打印 | Ctrl+P |
| ⚙️ | 设置 | Ctrl+, |
| ❓ | 帮助 | F1 |

**样式**: 图标 24×24px，深棕黑线条，悬停时琥珀金背景 + 深棕黑图标。

---

## 七、底部状态栏 (StatusBar)

**分区**（从左到右）：

```
[AKO Logo 16px]  当前项目: 万峰林民宿  |  计算状态: ✓ 已同步  |  内墙: 125.5㎡ 外墙: 86.0㎡  |  总价: ¥73,807  |  就绪
```

- 项目名: 点击可快速编辑
- 计算状态: 工程量修改后显示「⚠ 待重新计算」，点击后跳转标签页二
- 总价: 熔金色，始终显示最新值
- **错误提示区**: 文件读写异常时，状态栏右侧显示红色文字错误信息（非阻塞 QMessageBox）

---

## 八、数据流架构（v1.1 修订版）

### 8.1 三层架构 + 后台线程

```
┌─────────────────────────────────────────────────────────────┐
│                        用户交互层 (UI Thread)               │
│  (QLineEdit / QTableView / QComboBox / QPushButton)         │
│  职责: 接收输入、展示结果、不阻塞                             │
└──────────────────────────┬──────────────────────────────────┘
                           │ Signal/Slot (跨线程安全)
┌──────────────────────────▼──────────────────────────────────┐
│                      后台计算引擎 (QThread)                   │
│  (CalculationEngine: 遍历树+表格汇总+税率重算)              │
│  职责: 所有重计算在独立线程执行，完成后 emit 结果信号         │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                      数据持久层 (Workspace)                   │
│  (全量 JSON: project.json / 模板 JSON / 配置 config.json)   │
│  职责: 原子写入、版本控制、Git 友好                            │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 后台线程设计（新增）

**CalculationWorker (QThread 子类)**:

```python
# 伪代码示意（VS Code 实现参考）
class CalculationWorker(QThread):
    result_ready = Signal(dict)  # 计算完成信号，携带结果字典
    progress = Signal(int)      # 进度百分比

    def __init__(self, project_data):
        super().__init__()
        self.project_data = project_data

    def run(self):
        # 1. 遍历所有房间工程量
        # 2. 汇总内墙/外墙面积
        # 3. 套用单价 + 附加费 + 税率
        # 4. emit result_ready({"total": 73807, "breakdown": {...}})
        pass
```

**调用流程**:
1. 用户修改工程量 → UI 触发 `QTimer::singleShot(300ms, self.start_calculation)`
2. 300ms 后无新输入 → 创建 `CalculationWorker` 实例 → `worker.start()`
3. UI 显示进度条（或状态栏「计算中...」）
4. Worker 完成 → `result_ready` 信号 → UI 更新总价、饼图、状态栏
5. Worker 自动 `deleteLater()`，防止内存泄漏

**铁律**: 严禁在主线程执行超过 50ms 的循环计算。

### 8.3 数据持久化：Workspace 模式（v1.1 重大修订）

**放弃 SQLite，全面转向 JSON**:

| 原方案 (v1.0) | 修订方案 (v1.1) | 理由 |
|---------------|----------------|------|
| SQLite 存元数据 | **全部存 JSON** | Git 版本控制友好、无 Schema 迁移、打包体积小 |
| JSON 存详细数据 | **Workspace 文件夹** | 每个项目独立文件夹，含全量数据 + 缓存 |

**Workspace 目录结构**:

```
~/AKO_Projects/
├── 万峰林民宿_20260726/
│   ├── project.json          # 全量数据（项目信息+工程量+报价明细）
│   ├── project.json.bak      # 自动备份（每次保存前复制）
│   ├── thumb.png             # 缩略图/封面（可选）
│   └── exports/              # 导出文件目录
│       ├── 报价单_正式.pdf
│       └── 明细表.xlsx
├── 桐木岭商业_20260720/
│   ├── project.json
│   └── ...
└── _templates/               # 模板库
    ├── 标准住宅.json
    ├── 商业综合体.json
    └── 民宿改造.json
```

**project.json 数据模型（含版本控制）**:

```json
{
  "schema_version": "1.1",
  "project_id": "ako-prj-20260726-001",
  "created_at": "2026-07-26T10:30:00+08:00",
  "modified_at": "2026-07-26T14:20:00+08:00",
  "project_info": {
    "name": "万峰林民宿",
    "address": "兴义市万峰林景区",
    "type": "民宿",
    "area": 450,
    "floors": 2
  },
  "tech_params": {
    "standard": "T/CECS 10154-2021",
    "density": 138,
    "compressive": "5-10",
    "soundproof": 46,
    "fireproof": "≥4h"
  },
  "quantity": {
    "rooms": [
      {
        "room_id": "r001",
        "floor": "1F",
        "name": "客厅",
        "items": [
          {"item_id": "i001", "wall_type": "内墙", "length": 12.5, "height": 3.0, "area": 37.5}
        ]
      }
    ],
    "summary": {
      "inner_wall": 125.5,
      "outer_wall": 86.0,
      "deduction": 23.5,
      "net": 188.0,
      "waste_factor": 1.05,
      "actual": 197.4
    }
  },
  "quotation": {
    "items": [
      {"seq": 1, "item": "内墙板", "unit": "㎡", "qty": 125.5, "price": 122, "amount": 15311}
    ],
    "subtotal": 67713,
    "tax_rate": 0.09,
    "tax_amount": 6094,
    "total": 73807,
    "locked": false,
    "quote_no": "AKO-Q-20260726-001"
  }
}
```

**JSON 版本控制策略**:
- `schema_version`: 字符串，当前 `"1.1"`
- 程序启动时读取 JSON，若 `schema_version` 不匹配 → 弹出升级对话框或拒绝加载
- 每次保存前，自动复制当前 `project.json` 为 `project.json.bak`
- 导航树通过扫描 `~/AKO_Projects/` 下所有含 `project.json` 的文件夹生成，无需数据库

---

## 九、打包配置（PyInstaller）

### 9.1 auto-py-to-exe 配置

| 配置项 | 值 |
|--------|-----|
| Script | `AKO_quote_agent_gui.py` |
| Onefile | ✅ One File |
| Console | ✅ Window Based (hide console) |
| Icon | `ako_logo.ico` |
| Additional Files | `config.json`, `templates/`, `assets/`, `AKO_quote_agent_GUI_Whitepaper_v1.1.md` |
| UPX | ✅ 压缩 |
| Output | `E:\AKO\dist\` |

### 9.2 命令行备用

```bash
pyinstaller \
  --onefile \
  --noconsole \
  --icon=ako_logo.ico \
  --add-data "config.json;." \
  --add-data "templates;templates" \
  --add-data "assets;assets" \
  --name "AKO_quote_agent_v1.1" \
  AKO_quote_agent_gui.py
```

### 9.3 体积预估

| 组件 | 体积贡献 |
|------|----------|
| PySide6 核心 | ~45MB |
| QtCharts | ~8MB |
| QPdfView | ~5MB |
| Python 运行时 | ~12MB |
| 业务代码+资源 | ~3MB |
| **总计** | **~75MB** |

---

## 十、开发优先级（VS Code 执行顺序）

| 阶段 | 任务 | 产出 |
|------|------|------|
| P0 | 搭建 PySide6 主窗口框架 + 高 DPI 适配 | 可运行的空窗口，带 AKO 品牌色 |
| P1 | 实现 Workspace 扫描 + 左侧导航树 | 能识别 `~/AKO_Projects/` 下的项目 |
| P2 | 实现「项目信息」标签页 + JSON 读写 | 表单录入 + 保存到 project.json |
| P3 | 实现「工程量计算」标签页 + ProxyModel + 防抖 | 表格增删改 + 自动计算面积 |
| P4 | 实现 CalculationWorker (QThread) | 后台计算引擎，UI 不卡死 |
| P5 | 实现「报价明细」标签页 + 饼图钻取 | 单价套用 + 税率计算 + 总价展示 |
| P6 | 实现「预览输出」标签页 + WYSIWYG | QTextDocument → QPrinter → QPdfView |
| P7 | 实现 QFileSystemWatcher 外部监控 | 外部修改自动检测 + 刷新提示 |
| P8 | 打包测试 | auto-py-to-exe 生成 exe，无 Python 环境验证 |

---

## 十一、与 AKO 体系的联动

1. **AKO_hub**: 报价完成后，自动向 hub 推送项目摘要（项目名、总价、状态）
2. **AKO_tag_manager**: 报价单 PDF 自动打标签 `#业务/报价/已完成`
3. **AKO_law_agent**: 报价单中的技术参数引用宪法中的标准条款编号
4. **品牌一致性**: 所有输出文档（PDF/Excel）页眉必须带 AKO Logo + 公司全称

---

## 十二、代码生成约束（AI Compiler Rules）

> 本章专门约束 VS Code Copilot / Cursor 的代码生成行为，防止常见 Qt 开发陷阱。

### 12.1 Signal/Slot 连接规范

- **禁止使用 lambda 直接连接 Signal/Slot**
- 必须使用 `functools.partial` 或自定义 `@Slot` 装饰器方法
- 原因: lambda 捕获循环变量或 self 引用时，极易造成循环引用，导致对象无法被垃圾回收，内存泄漏

```python
# ❌ 禁止
button.clicked.connect(lambda: self.calculate(room_id))

# ✅ 允许
from functools import partial
button.clicked.connect(partial(self.calculate, room_id))

# ✅ 允许（推荐）
@Slot()
def on_calculate_clicked(self):
    self.calculate(self.current_room_id)
button.clicked.connect(self.on_calculate_clicked)
```

### 12.2 表格组件强制规范

- **所有表格必须使用 QTableView + QStandardItemModel**
- **严禁混用 QTableWidget**
- 原因: QTableWidget 是 MVC 混合体，数据与视图耦合，无法使用 ProxyModel 过滤/排序，扩展性差

### 12.3 异常处理强制规范

- **所有文件读写（JSON/PDF）必须包裹 try-except**
- 错误信息必须显示在**状态栏**（红色文字），禁止弹出阻塞式的 `QMessageBox`
- 业务级工具应有非侵入式错误提示，不中断用户当前操作流

```python
# ✅ 允许
try:
    with open(path, 'w', encoding='utf-8') as f:
        json.dump(data, f, ensure_ascii=False, indent=2)
    self.statusBar().showMessage("保存成功", 3000)
except Exception as e:
    self.statusBar().showMessage(f"保存失败: {str(e)}", 5000)
    # 可选: 状态栏文字变红色 (通过 setStyleSheet)
```

### 12.4 线程安全强制规范

- **严禁从 Worker QThread 直接操作 UI 控件**
- Worker 必须通过 Signal 向主线程发送结果，主线程 Slot 负责更新 UI
- 严禁在 Worker 中调用 `self.parent().label.setText(...)`

### 12.5 资源释放强制规范

- 所有 QThread 实例在完成后必须调用 `worker.deleteLater()`
- 所有 QFileDialog / QPrintDialog 实例用完后置为 None，防止句柄泄漏
- 程序退出前，强制保存当前项目（若未锁定）

---

## 十三、终审评级（v1.1 修订后）

| 维度 | 评分 | 状态 |
|------|------|------|
| 视觉与品牌 | ⭐⭐⭐⭐⭐ | 完美，可直接交付 UI/UX |
| 组件选型 | ⭐⭐⭐⭐⭐ | 精准，PySide6 选型无争议 |
| 并发与性能 | ⭐⭐⭐⭐⭐ | **已补充 QThread + 防抖，通过** |
| 数据一致性 | ⭐⭐⭐⭐⭐ | **已改为 Workspace + 全量 JSON，通过** |
| 可测试性 | ⭐⭐⭐⭐☆ | Engine 与 View 已分离，Worker 可独立单元测试 |
| WYSIWYG 一致性 | ⭐⭐⭐⭐⭐ | 统一 QTextDocument + QPrinter 管线，通过 |
| 外部文件监控 | ⭐⭐⭐⭐⭐ | QFileSystemWatcher 已纳入，通过 |

**最终意见**: **通过（Approved）**。v1.1 已补齐所有架构级风险，可交由 VS Code 进行代码生成。

---

> **铁律提醒**: 本文档为 GUI 顶层设计白皮书，所有代码实现由 VS Code 执行。AI 仅输出界面架构、组件清单、交互逻辑、品牌规范与代码生成约束，不直接编写业务代码。

---

*AKO_studio | 2026-07-26*
