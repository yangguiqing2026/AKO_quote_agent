# AKO_login_guard 账号密码管理方案

> **文档作者**: AKO_studio  
> **版本**: v1.0  
> **日期**: 2026-07-26  
> **性质**: 账号生命周期管理操作手册，指导管理员（你）和 VS Code 实现  
> **适用对象**: AKO 内部员工（电脑盲），管理员（你）

---

## 一、核心原则

| 原则 | 说明 |
|------|------|
| **管理员集权** | 只有你能创建、重置、禁用账号。员工无权自行注册 |
| **零记忆负担** | 初始密码规则简单好记，首次登录强制修改为自己熟悉的密码 |
| **一次一密** | 重置密码后，旧密码立即失效，员工下次登录必须输新密码 |
| **可追溯** | 所有创建、修改、重置操作记录日志，写入 `audit.log` |
| **离线可用** | 不依赖任何网络服务，纯本地 SQLite 管理 |

---

## 二、账号命名规则

### 2.1 用户名（username）

**格式**: 员工姓名拼音全小写，无空格，无特殊符号

| 员工 | 用户名 |
|------|--------|
| 罗泽广 | `luozeguang` |
| 张三 | `zhangsan` |
| 王五 | `wangwu` |

**规则**:
- 全小写英文字母
- 无空格、无数字、无符号
- 重名时加序号：`zhangsan1`、`zhangsan2`
- 长度 4-20 字符
- 创建后**不可修改**（作为系统唯一标识）

### 2.2 显示名（display_name）

**格式**: 员工真实姓名，中文

| 员工 | 显示名 |
|------|--------|
| 罗泽广 | `罗泽广` |

**用途**: 登录后主窗口状态栏显示、报价单署名、操作日志记录

---

## 三、密码生命周期管理

### 3.1 初始密码生成策略

**方案 A（推荐）：统一初始密码 + 首次强制修改**

```
初始密码: ako + 员工手机号后4位

例: 罗泽广手机 13385118522 → 初始密码: ako8522
```

**优点**:
- 员工好记（自己的手机号后缀）
- 不同员工密码不同，防止一人泄露全员遭殃
- 长度 8 位以上，含字母+数字，满足基础复杂度

**方案 B（高安全）：随机生成 + 管理员告知**

```
初始密码: 随机8位字符串（如 x7Kp9mQ2）
管理员通过企业微信/短信/口头告知员工
```

**优点**: 安全性最高  
**缺点**: 员工记不住，首次登录后必须立即修改

**AKO 建议**: 小团队（<20人）用方案 A，方便；团队扩大后切方案 B。

### 3.2 首次登录强制改密码

**流程**:

```
员工首次登录（用初始密码）
    ↓
系统检测 "must_change_password" 标志位 = true
    ↓
不进入主程序，先弹「修改密码」窗口
    ↓
┌─────────────────────────────┐
│  首次登录，请修改密码          │
│                              │
│  新密码: [________]          │
│  确认密码: [________]        │
│                              │
│  密码要求:                   │
│  • 至少8位                   │
│  • 包含字母和数字             │
│  • 不可与初始密码相同         │
│                              │
│  [确认修改]                  │
└─────────────────────────────┘
    ↓
修改成功 → 进入主程序
    ↓
"must_change_password" 标志位设为 false
```

**密码复杂度校验**:
- 最少 8 位
- 必须同时包含英文字母 + 阿拉伯数字
- 不可与最近 3 次历史密码重复
- 不可与用户名相同

### 3.3 员工自助修改密码

**入口**: 主窗口菜单栏「系统」→「修改密码」

**流程**:

```
┌─────────────────────────────┐
│  修改密码                    │
│                              │
│  当前密码: [________]        │
│  新密码: [________]          │
│  确认新密码: [________]      │
│                              │
│  [确认]  [取消]              │
└─────────────────────────────┘
```

**规则**:
- 必须输入正确的当前密码才能修改
- 新密码复杂度同上
- 修改成功后，「记住我」Token 自动失效，下次需重新登录

### 3.4 管理员重置密码

**场景**: 员工忘记密码

**流程**:

```
管理员登录 → 「系统」→「用户管理」
    ↓
选中员工账号 → 点击「重置密码」
    ↓
弹窗确认: "确定重置 罗泽广 的密码？"
    ↓
系统生成新初始密码（ako + 手机号后4位）
    ↓
该账号的 "must_change_password" 标志位设为 true
    ↓
管理员将新密码告知员工
    ↓
员工下次登录 → 强制修改密码 → 正常使用
```

**注意**: 重置密码后，该员工的所有「记住我」Token 自动清除，所有设备需重新登录。

---

## 四、管理员操作界面

### 4.1 用户管理主界面

```
┌─────────────────────────────────────────────────────┐
│  AKO 用户管理（仅管理员可见）                        │
├─────────────────────────────────────────────────────┤
│  [搜索用户...]              [+ 添加用户]             │
├─────────────────────────────────────────────────────┤
│  ┌────┬──────────┬──────────┬──────┬────────────┐  │
│  │ 序号│ 用户名   │ 显示名   │ 角色 │ 状态       │  │
│  ├────┼──────────┼──────────┼──────┼────────────┤  │
│  │ 1   │ admin    │ 管理员   │ admin│ ● 在线     │  │
│  │ 2   │ luozeguang│ 罗泽广  │ user │ ● 在线     │  │
│  │ 3   │ zhangsan │ 张三     │ user │ ○ 离线     │  │
│  │ 4   │ wangwu   │ 王五     │ user │ ✕ 已禁用   │  │
│  └────┴──────────┴──────────┴──────┴────────────┘  │
│                                                     │
│  [重置密码] [启用/禁用] [删除] [查看日志]           │
└─────────────────────────────────────────────────────┘
```

### 4.2 添加用户窗口

```
┌─────────────────────────────┐
│  添加新用户                  │
│                              │
│  用户名: [________]          │
│  显示名: [________]          │
│  手机号: [________]          │  ← 用于生成初始密码
│  角色:   [user ▼]            │  ← admin / user
│                              │
│  初始密码将自动生成: akoXXXX   │
│  （XXXX为手机号后4位）        │
│                              │
│  [确认添加]  [取消]           │
└─────────────────────────────┘
```

**添加后自动执行**:
1. 生成 bcrypt 哈希存入数据库
2. `must_change_password = true`
3. 记录审计日志：`[2026-07-26 14:30] admin 创建用户 luozeguang`
4. 弹窗显示初始密码，提示管理员告知员工

### 4.3 批量导入（可选）

**场景**: 一次招了 5-10 个新员工

**方式**: 准备一个 `employees.csv` 文件：

```csv
username,display_name,phone,role
luozeguang,罗泽广,13385118522,user
zhangsan,张三,13912345678,user
wangwu,王五,13887654321,user
```

**操作**: 用户管理界面 → 「批量导入」→ 选择 CSV → 系统自动创建所有账号 → 生成密码清单（可打印或导出 Excel）

---

## 五、密码存储安全规范

### 5.1 哈希算法

**必须使用 bcrypt**，参数：
- rounds = 12（计算耗时约 250ms，防暴力破解）
- 严禁使用 MD5、SHA1、SHA256（太快了，易被彩虹表攻击）

```python
# 伪代码（VS Code 实现参考）
import bcrypt

# 注册/重置时：明文 → 哈希
password = "ako8522"
hashed = bcrypt.hashpw(password.encode(), bcrypt.gensalt(rounds=12))
# 存入数据库: hashed.decode()

# 登录时：验证
if bcrypt.checkpw(input_password.encode(), stored_hash.encode()):
    # 密码正确
```

### 5.2 数据库加密

SQLite 数据库文件 `users.db` 使用 SQLCipher 加密：

```python
PRAGMA key = 'AKO_DB_KEY_2026'
```

加密密钥硬编码在代码中（PyInstaller 打包后难以提取）。

### 5.3 审计日志

所有账号操作写入 `~/.ako/audit.log`：

```
[2026-07-26 14:30:15] admin CREATE user=luozeguang display_name=罗泽广
[2026-07-26 15:10:22] admin RESET_PASSWORD user=zhangsan
[2026-07-26 16:45:08] luozeguang LOGIN success ip=192.168.1.105
[2026-07-26 17:20:33] wangwu LOGIN failed (3/3) LOCKOUT
[2026-07-26 17:21:01] admin DISABLE user=wangwu
```

日志文件只追加、不删除，管理员可查看但不可修改。

---

## 六、员工视角使用流程

### 6.1 首次使用（管理员已创建账号）

```
1. 收到管理员通知：
   "你的账号是 luozeguang，初始密码是 ako8522"

2. 双击 AKO_quote_agent.exe

3. 弹出登录窗口
   用户名: luozeguang
   密码: ako8522
   [ ] 记住我（暂不勾，先改密码）
   → 点击「登录」

4. 系统提示：首次登录，请修改密码
   新密码: （员工自己设一个熟悉的，如生日+公司简称）
   确认密码: 
   → 点击「确认修改」

5. 进入 AKO_quote_agent 主界面，正常使用

6. 关闭程序，下次双击
   → 重新输密码（如果之前没勾记住我）
   → 或直接进主界面（如果勾了记住我）
```

### 6.2 忘记密码

```
1. 员工找你（管理员）："我忘记密码了"

2. 你登录自己的 admin 账号

3. 「系统」→「用户管理」→ 找到该员工 → 「重置密码」

4. 系统生成新初始密码（如 ako6688）

5. 你口头/微信告知员工："新密码是 ako6688，登录后自己改"

6. 员工重新登录 → 强制修改密码 → 正常使用
```

---

## 七、角色与权限

| 角色 | 权限 |
|------|------|
| **admin** | 用户管理（增删改）、查看审计日志、修改所有设置、使用全部 Agent 功能 |
| **user** | 仅使用 Agent 业务功能、修改自己的密码、查看自己的操作记录 |

**默认角色分配**:
- 你（创始人）：`admin`
- 罗泽广等核心员工：`user`（后续可提拔为 admin）
- 临时/实习生：可创建 `user` 角色，项目结束后禁用

---

## 八、应急方案

### 8.1 admin 密码也忘了

**方案**: 物理删除 `users.db` 文件，程序下次启动自动重建，恢复默认 `admin` / `ako2026`

**操作**:
```
关闭程序
删除 C:\Users\{用户名}\.ako\users.db
重新双击 .exe
用 admin / ako2026 登录
重新添加所有员工账号
```

**风险**: 所有员工账号需重新创建。建议 admin 密码写在你自己的 Obsidian 里备份。

### 8.2 员工离职

**操作**: 管理员登录 → 用户管理 → 选中该员工 → 「禁用」

**效果**: 该账号立即无法登录，已发出的「记住我」Token 失效。数据保留，随时可重新启用。

**彻底删除**: 禁用 30 天后，管理员可执行「删除」，物理清除账号记录。

---

## 九、VS Code 实现要点

### 9.1 数据表扩展

在 `AKO_login_guard` 用户表基础上增加字段：

```sql
ALTER TABLE users ADD COLUMN phone TEXT;              -- 手机号（用于生成初始密码）
ALTER TABLE users ADD COLUMN must_change_password INTEGER DEFAULT 1;  -- 首次登录强制改密
ALTER TABLE users ADD COLUMN password_history TEXT;     -- JSON 数组，存最近3次密码哈希
ALTER TABLE users ADD COLUMN failed_attempts INTEGER DEFAULT 0;  -- 连续失败次数
ALTER TABLE users ADD COLUMN locked_until TEXT;         -- 锁定截止时间（ISO 8601）
```

### 9.2 关键代码约束

1. **创建用户时**: 必须同时生成 bcrypt 哈希 + 设置 `must_change_password = 1`
2. **登录验证时**: 先查 `is_active = 1`，再查 `locked_until` 是否过期，最后 bcrypt 比对
3. **修改密码时**: 新密码必须通过复杂度校验，存入 `password_history`，更新 `must_change_password = 0`
4. **重置密码时**: 清除该用户所有 Token，设置 `must_change_password = 1`，`failed_attempts = 0`

---

> **铁律提醒**: 本文档为规划级操作手册，所有代码实现由 VS Code 执行。AI 仅输出管理流程、安全规范与数据模型，不直接编写业务代码。

---

*AKO_studio | 2026-07-26*
